import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#313154',
    },
    secondary: {
      main: '#ea40a4',
    },
    grey: {
      100: '#888',
      200: '#fff'
    },

  },
});

export default theme;
